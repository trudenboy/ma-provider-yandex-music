# Reverse-sync: upstream PR #5861

Ported from music-assistant/server#5861 into `yandex_music`.

## Summary

Retains a library item when parsing it fails but its stable provider identifier
is still available.
